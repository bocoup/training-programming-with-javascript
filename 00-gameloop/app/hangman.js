/**
 * Continuously update the game board.
 *
 * Implementation:
 *   1. Create a variable, `game` and set it to the element
 *      in the DOM with an ID of "canvas".
 *   2. Set the innerHTML of the `game` to `Math.random()`.
 */
var refresh = function () {

};

setInterval(refresh, 100);
